const sequelize = require("../../../config/db");
const { ReplaceWithService } = require("../../../service/v1");

const {{camelCase name}} = async (req, res, next) => {
try {
let data = [];
await sequelize.transaction(async t1 => {
data = await new ReplaceWithService().tempService(req.body, req.user);
});
return data;
} catch (err) {
next(err);
}
};

module.exports = {
{{camelCase name}}
};
